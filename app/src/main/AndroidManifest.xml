<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abelherl.antrian">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AdminCreateQue"></activity>
        <activity android:name=".AdminUpdateAct" />
        <activity android:name=".AdminCreateQueue" />
        <activity android:name=".AdminCreateAct" />
        <activity android:name=".AdminManageQueue" />
        <activity android:name=".AdminManageRequest" />
        <activity android:name=".AdminManageActivity" />
        <activity android:name=".AdminAdminHome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>